import React from "react";
import { FormattedMessage } from "react-intl";

export const APPLICATION_NAME_LABEL = (
  <FormattedMessage
    id="overview.application-name"
    defaultMessage={"Application name"}
  />
);
