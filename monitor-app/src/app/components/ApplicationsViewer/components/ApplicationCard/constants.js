import React from "react";
import { FormattedMessage } from "react-intl";

export const EDIT_APPLICATION_LABEL = (
  <FormattedMessage id="overview.edit" defaultMessage={"Edit"} />
);

export const ENDPOINT_APPLICATION_LABEL = (
  <FormattedMessage id="overview.endpoint" defaultMessage={"Endpoint"} />
);
